import React from "react";

type TitleProps = {
    title: string;
};

function SelectedTab({title}: TitleProps) {
    return (
        <button>
        m a button
        </button>
    );
}

export default SelectedTab;
